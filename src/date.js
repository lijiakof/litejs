/** @module date */

export default {
    /**
     * 获取当前时间 测试
     * @param { String } accurate - 时间精度
     * @return { String } 当前的时间
     */
    now: function () { },
    /**
     * 格式化 Date 
     * @param { Date } date - 传入的日期
     * @param { String } fmtString - 格式化字符串
     * @return { String } 格式化后的字符串
     */
    format: function (date, fmtString) {

    },
    add: function () { },
    isLeapYear: function () { },
    daysInMonth: function () { },
    dayOfYear: function () { },
    equal: function () { },
    diff: function () { }
};